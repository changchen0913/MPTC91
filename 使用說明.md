# 91 週年紀念禮盒

使用 Three.js 製作的互動網頁。所有圖片與 3D 函式庫均已放在資料夾內，預覽時不需要下載外部素材。

## 開啟網頁

雙擊「開啟禮盒.cmd」即可啟動本機網頁。需要 Node.js 20 或以上；這台電腦已安裝。

也可在這個資料夾執行 `npm start`，再開啟 http://127.0.0.1:4173 。請以網頁伺服器開啟，直接雙擊 HTML 無法載入 3D 模組。

## 操作

- 直接拖曳盒蓋開闔，盒蓋會跟著游標走；放手後依拖曳方向與幅度自動開到底或闔上。輕點盒蓋也可直接開關。
- 拖曳盒蓋以外的地方旋轉視角，滾輪縮放。
- 開盒後點選產品拿起特寫；特寫時拖曳可旋轉產品，「放回禮盒」或 Esc 返回。
- 滑鼠墊可以平滑展開與捲起。
- 畫面上不放操作按鈕；鍵盤操作者按 Tab 會叫出開闔與選擇產品的按鈕列。

## 尺寸

- 外盒：15 × 26.5 × 高 10 公分。
- 咖啡包：10.1 × 8.2 公分，參考圖為 6 入。
- 玻璃杯：直徑 8.2 × 高 9.5 公分。
- 滑鼠墊：25 × 30 公分。

盒蓋圖案取自平面美工稿 `assets/cover.png`，依寬度等比套用，黑底去背後由盒身材質透出，兩端留黑。

模型統一使用每公分 0.25 場景單位。外盒壁厚、包裝厚度、杯壁厚度、滑鼠墊厚度及內襯間距為展示用估算，並非製造圖。滑鼠墊原圖為正方形，展示貼圖依指定矩形比例延展；未生成未提供的背面圖案。玻璃杯山形由立體曲面重建，反光以即時渲染近似參考圖。

## 網頁檔案

`docs` 是完整靜態網站；可部署在支援靜態網站的伺服器，也是 GitHub Pages 的發佈來源。`app.js` 中的 `data` 集中管理產品介紹與尺寸。圖片位於 `docs/assets`。
